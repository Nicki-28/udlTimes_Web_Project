version: "3.9"

services:
  web:
    build: .
    command: uvicorn udlTimes_Web_Project.asgi:application --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - DEBUG=1
