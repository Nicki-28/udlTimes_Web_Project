[project]
name = "udltimes-web-project"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.2",
    "uvicorn[standard]",
    "djangorestframework",
    "python-decouple>=3.8",
    "psycopg[binary]>=3.1",
]
