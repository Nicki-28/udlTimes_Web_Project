[project]
name = "udltimes-web-project"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "django>=5.0",
    "uvicorn[standard]",
    "djangorestframework",
    "python-decouple>=3.8",
    "psycopg[binary]>=3.1",
    "flask>=3.1.3",
    "flask-cors>=6.0.2",
]
